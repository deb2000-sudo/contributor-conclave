import { LoadingState } from "@/components/ui/loading-state";

export default function MentorLoading() {
  return (
    <main>
      <LoadingState label="Loading the mentor workspace" />
    </main>
  );
}
